# homebrew-tap

Homebrew tap for the [Ducky](https://tryducky.dev) CLI.

```bash
brew install neocho/tap/ducky
```

Installs the prebuilt `ducky` binary for macOS (Apple Silicon + Intel) from this tap's `cli-v*` releases.
